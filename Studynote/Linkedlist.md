## 定義
由節點 (Node) 組成的線性結構，每個節點包含資料和指向下一個節點的指標 (Pointer)。
## 特性
- 動態大小：記憶體不需連續，可隨時擴充。
- 循序存取：不支援隨機存取，需從頭遍歷。
## 時間複雜度
- Access (讀取)：O(n)
- Search (搜尋)：O(n)
- Insertion (插入)：O(1) (已知位置)
- Deletion (刪除)：O(1) (已知位置)
## 優缺點
- 優點：插入與刪除效率高、不需預先分配固定大小。
- 缺點：讀取速度較慢 (不支援 Random Access)、需額外記憶體儲存指標。
